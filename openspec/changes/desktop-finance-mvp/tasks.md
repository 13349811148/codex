# Tasks

## 1. Project Skeleton
- [x] 1.1 创建 `Python + PySide6` 项目骨架
- [x] 1.2 创建入口、主窗口、服务层和解析器结构
- [x] 1.3 接入 `SQLite` 配置与运行日志

## 2. File Import
- [x] 2.1 实现目录扫描
- [x] 2.2 支持 `CSV/XLS/XLSX`
- [x] 2.3 实现文件名中的平台与店铺解析
- [x] 2.4 实现来源类型识别
- [x] 2.5 支持目录中自动忽略非账单模板文件

## 3. Parsing
- [x] 3.1 实现 `拼多多 CSV` 解析器
- [x] 3.2 实现 `淘宝 XLSX/XLS` 解析器
- [x] 3.3 过滤空白尾行
- [x] 3.4 扩展为 `拼多多 / 淘宝系` 同时支持 `CSV/XLS/XLSX`
- [x] 3.5 改为自动定位表头，不依赖固定行号

## 4. Normalization
- [x] 4.1 实现金额标准化
- [x] 4.2 实现月份提取
- [x] 4.3 实现淘宝备注归一化
- [x] 4.4 淘宝备注归一化扩展到 `天猫 / 淘工厂 / 淘农场`

## 5. Classification
- [x] 5.1 实现淘宝 `备注 —— 业务描述` 组合键分类
- [x] 5.2 实现拼多多 `业务描述` 分类
- [x] 5.3 支持 `[空] —— [空]` 忽略逻辑
- [x] 5.4 补充已确认的拼多多规则
- [x] 5.5 支持 `天猫 / 淘工厂 / 淘农场` 复用淘宝系规则
- [x] 5.6 未命中分类规则时自动归为 `暂未分类`
- [x] 5.7 导出后弹窗提醒未映射业务项
- [x] 5.8 根据 `分类映射2.xlsx` 更新淘宝系映射

## 6. Aggregation And Export
- [x] 6.1 实现分类汇总长表
- [x] 6.2 输出 `大类` 与 `明细分类`
- [x] 6.3 导出单个 `Excel` 文件
- [x] 6.4 导出前弹出保存文件对话框
- [x] 6.5 按贷借法输出 `收入金额` 与 `支出金额`
- [x] 6.6 调整拼多多 `虚假发货`、`运费补偿` 到 `售后费用`
- [x] 6.7 未映射记录仍参与汇总并显示为 `暂未分类`

## 7. Validation
- [x] 7.1 安装 `Python 3.11`
- [x] 7.2 安装项目依赖
- [x] 7.3 用真实样本生成第一份汇总报表
- [x] 7.4 启动 `PySide6` 界面验证可打开
- [x] 7.5 用真实样本验证贷借法双金额列输出
- [x] 7.6 用真实样本验证拼多多两项售后费用归类
- [x] 7.7 用反向格式样本验证同平台双格式导入
- [x] 7.8 用未映射样本验证 `暂未分类` 与提醒弹窗
- [x] 7.9 验证后台处理进度回调链路

## 8. UI And Packaging
- [x] 8.1 处理大数据量时改为后台线程执行
- [x] 8.2 增加处理进度文本与进度条
- [x] 8.3 异常信息支持复制选中 / 复制全部
- [x] 8.4 生成无控制台桌面程序
- [x] 8.5 生成 `V1.2` 安装包并支持覆盖升级

## 9. Online Mapping
- [x] 9.1 新增本地映射缓存与同步元数据存储
- [x] 9.2 新增 `手动同步映射` 界面入口
- [x] 9.3 新增 `WPS OAuth + OpenAPI` 在线映射同步链路
- [x] 9.4 新增 CLI 工具查看状态、授权、预览与同步
- [x] 9.5 支持手动写入 `wps_file_id` 作为私有短链兜底
- [x] 9.6 改为按工作表 `active_area` 读取有效区域
- [x] 9.7 用真实在线表格完成 `124` 条规则同步验证

## 10. Next Work
- [ ] 10.1 增加软件内配置 `wps_file_id` 的界面，减少命令行依赖
- [ ] 10.2 自动过滤金额为 `0` 的空记录，减少异常噪音
- [ ] 10.3 增加导出成功后打开目录的能力
- [ ] 10.4 增加规则管理界面
