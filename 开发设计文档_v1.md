# 开发设计文档 v1.1

## 项目名称
财务统计小工具

## 对应 PRD
`PRD v1.3`

## 文档日期
`2026-04-19`

## 1. 设计目标
本设计文档描述财务统计小工具当前可运行版本的实现方案，重点覆盖：

- Windows 单机桌面应用
- 批量读取 `CSV/XLS/XLSX`
- 从文件名识别平台与店铺
- 按平台规则分类
- 生成分类汇总报表
- 导出 Excel

当前版本已经落地两项关键输出规则：

1. 导出表采用“贷借法”双金额列：
   - `收入金额`
   - `支出金额`
2. 导出表保留分类字段：
   - `大类`
   - `明细分类`

## 2. 技术选型

- `Python 3.11+`
- `PySide6`
- `pandas`
- `openpyxl`
- `SQLite`

说明：
- `PySide6` 负责桌面 GUI
- `pandas` 负责读取、清洗、汇总
- `openpyxl` 负责导出 Excel
- `SQLite` 负责保存配置与运行日志

## 3. 系统结构

系统采用四层结构：

1. `UI 层`
   - 主窗口
   - 数据目录选择
   - 导出保存对话框
   - 运行结果和异常展示

2. `服务层`
   - 运行总流程编排
   - 导入、分类、汇总、导出调度

3. `领域层`
   - 文件名解析
   - 来源识别
   - 金额标准化
   - 备注归一化
   - 分类映射
   - 汇总计算

4. `基础设施层`
   - 文件系统访问
   - CSV/XLS/XLSX 读取
   - SQLite
   - Excel 导出

## 4. 项目结构

```text
finance_tool/
  app/
  ui/
  services/
  parsers/
  domain/
  repositories/
  models/
  db/
  exporters/
  utils/
  main.py
```

## 5. 核心运行流程

```text
选择数据目录
-> 选择导出文件位置
-> 扫描支持文件
-> 按文件名识别平台和店铺
-> 识别来源模板
-> 调用对应解析器
-> 标准化时间、金额、备注
-> 分类映射
-> 跳过忽略记录
-> 生成分类汇总表
-> 导出 Excel
-> 返回运行结果
```

## 6. 解析设计

### 6.1 文件名解析
从文件名提取：

- `platform`
- `store_name`

当前支持平台关键词：

- `拼多多`
- `淘宝`
- `天猫`
- `京东`

示例：

- `3月拼多多—黄小惠米面旗舰店.csv`
  - 平台：`拼多多`
  - 店铺名：`黄小惠米面旗舰店`
- `3月淘宝-庄品健大米1号店（下载）.xlsx`
  - 平台：`淘宝`
  - 店铺名：`庄品健大米1号店`

### 6.2 来源识别

当前支持两类来源：

- `pdd_csv`
- `tb_xlsx`

识别依据：

- 文件扩展名
- 表头特征
- sheet 名

### 6.3 解析器

#### 拼多多 CSV
- 跳过前 4 行说明
- 第 5 行作为表头
- 读取：
  - `商户订单号`
  - `发生时间`
  - `收入金额（+元）`
  - `支出金额（-元）`
  - `账务类型`
  - `备注`
  - `业务描述`

#### 淘宝 XLSX/XLS
- 读取 `原始数据` sheet
- 第 3 行作为表头
- 读取：
  - `商户订单号`
  - `入账时间`
  - `收入（+元）`
  - `支出（-元）`
  - `账务类型`
  - `备注`
  - `业务描述`

## 7. 标准化设计

### 7.1 月份

- 拼多多：从 `发生时间` 提取 `YYYY-MM`
- 淘宝：从 `入账时间` 提取 `YYYY-MM`

### 7.2 金额

标准金额仍在内部统一为单个 signed amount：

- 收入：正数
- 支出：负数

异常规则：

- 收入和支出同时为空或为 `0`：记异常并跳过
- 收入和支出同时有值：记异常并跳过

### 7.3 淘宝备注归一化

目标：
- 去掉动态编号、订单号、邮箱等噪音
- 保留可用于分类的稳定文本

示例：

- `基础软件服务费(2701768802023008263)扣款`
- 归一化后：`基础软件服务费扣款`

## 8. 分类设计

### 8.1 淘宝

分类键：

`备注归一化 —— 业务描述`

示例：

- `基础软件服务费扣款 —— 0030130|软件服务费-基础软件服务费`
- `[空] —— 0010001|交易收款-交易收款`

特殊规则：

- `[空] —— [空]` 记为忽略
- 不进入汇总

### 8.2 拼多多

分类键优先使用：

- `业务描述`

已确认重点规则包括：

- `0010002|交易收入-订单收入` -> `销售收入 / 资金到账`
- `0010005|交易收入-优惠券结算` -> `销售收入 / 资金到账`
- `0130005|其他收入-申诉补回` -> `销售收入 / 资金到账`
- `0090001|平台补贴-补贴汇入` -> `补贴金额 / 资金到账`
- `0030001|技术服务费-技术服务费` -> `技术服务费 / 店铺费用`
- `0030002|技术服务费-基础技术服务费` -> `基础软件服务费 / 店铺费用`
- `0030023|技术服务费-黑标技术服务费` -> `技术服务费 / 店铺费用`
- `0030003|技术服务费-百亿补贴技术服务费` -> `技术服务费 / 店铺费用`
- `0050002|服务支出-消费者体验提升计划` -> `消费者体验提升计划 / 店铺费用`
- `0060001|营销费用-多多进宝` -> `多多进宝 / 广告费`
- `0020002|交易退款-订单退款` -> `售后退款 / 资金到账`
- `0020005|交易退款-优惠券结算` -> `售后退款 / 资金到账`
- `0040002|售后费用-售后补偿消费者` -> `售后补偿 / 店铺费用`
- `0040003|售后费用-运费补偿` -> `运费补偿 / 售后费用`
- `0040005|售后费用-虚假发货` -> `虚假发货 / 售后费用`
- `0080001|提现-提现申请` -> `资金提现 / 资金提现`

说明：
- `运费补偿` 和 `虚假发货` 已按最新要求调整为 `售后费用`

## 9. 汇总设计

当前导出采用“分类汇总长表”结构，每行代表一个分类维度组合：

- `月份`
- `店铺名`
- `平台`
- `大类`
- `明细分类`
- `收入金额`
- `支出金额`
- `记录数`

### 9.1 贷借法输出规则

内部仍然使用单个 signed amount 计算，但导出时拆分为两列：

- 如果金额大于 0，则：
  - `收入金额 = amount`
  - `支出金额 = 0`
- 如果金额小于 0，则：
  - `收入金额 = 0`
  - `支出金额 = abs(amount)`

这样可满足财务侧“收入/支出双列”展示要求。

### 9.2 记录数定义

`记录数` 指参与当前分类汇总行统计的原始明细条数：

- 仅统计成功解析且成功分类的记录
- 忽略记录不计入
- 异常记录不计入

## 10. 导出设计

### 10.1 导出方式

- 每次运行前弹出保存对话框
- 用户自行选择导出位置和文件名

### 10.2 导出文件

- 单个 Excel 文件
- 单个 sheet：`总汇总表`

### 10.3 导出列

- `月份`
- `店铺名`
- `平台`
- `大类`
- `明细分类`
- `收入金额`
- `支出金额`
- `记录数`

## 11. 数据存储

SQLite 当前保存：

- `app_config`
- `mapping_rules`
- `run_logs`

主要用途：

- 默认输入目录
- 最近一次导出目录
- 运行日志

## 12. 异常处理

异常类型包括：

- 文件打开失败
- 来源模板未识别
- 文件名解析失败
- 时间字段缺失
- 金额字段异常
- 分类规则未命中

策略：

- 单条异常不影响整文件
- 单文件异常不影响整批处理
- 所有异常显示在 UI 中

## 13. 当前验证结论

当前版本已经完成并验证：

- PySide6 界面可正常启动
- 可读取淘宝和拼多多真实样本
- 可导出包含 `大类` 和 `明细分类` 的长表
- 可按贷借法输出 `收入金额 / 支出金额`
- 拼多多 `虚假发货` 与 `运费补偿` 已归入 `售后费用`

## 14. 后续建议

建议后续继续推进：

1. 把分类规则从代码迁到 Excel 或数据库配置
2. 将金额为 `0` 的空记录自动忽略，减少异常提示
3. 增加导出后自动打开目录
4. 增加规则管理界面
