# 开发设计文档 v1.2

## 项目名称
财务统计小工具 V1.2

## 对应 PRD
`PRD v1.3`

## 文档日期
`2026-04-19`

## 1. 当前版本概要
本版本已经从最初 MVP 扩展为可分发的 `Windows` 单机桌面工具，当前已落地的关键能力包括：

- 支持 `拼多多 / 淘宝 / 天猫 / 淘工厂 / 淘农场`
- 各平台均支持 `CSV/XLS/XLSX`
- 从文件名识别平台与店铺
- 淘宝系按 `备注 —— 业务描述` 分类
- 拼多多按 `业务描述` 分类
- 未匹配映射自动归类为 `暂未分类`
- 导出后对未映射项弹窗提醒
- 汇总表输出 `大类`、`明细分类`
- 汇总表按贷借法输出 `收入金额 / 支出金额`
- 大批量数据后台处理并显示进度
- 异常信息支持复制选中 / 复制全部
- 生成单文件安装包并支持覆盖升级旧版本

## 2. 技术选型

- `Python 3.11+`
- `PySide6`
- `pandas`
- `openpyxl`
- `SQLite`
- `PyInstaller`
- `Inno Setup`

说明：
- `PySide6` 负责桌面 GUI 和后台线程交互
- `pandas` 负责读取、清洗、汇总
- `openpyxl` 负责导出 Excel
- `SQLite` 负责保存配置与运行日志
- `PyInstaller + Inno Setup` 负责打包发布

## 3. 系统结构

系统采用四层结构：

1. `UI 层`
   - 主窗口
   - 数据目录选择
   - 导出保存对话框
   - 进度条与进度文本
   - 异常与提醒展示
   - 异常复制

2. `服务层`
   - 运行总流程编排
   - 导入、分类、汇总、导出调度
   - 运行进度回调

3. `领域层`
   - 文件名解析
   - 来源识别
   - 金额标准化
   - 备注归一化
   - 分类映射
   - 汇总计算

4. `基础设施层`
   - 文件系统访问
   - CSV/XLS/XLSX 读取
   - SQLite
   - Excel 导出
   - 安装包构建

## 4. 核心运行流程

```text
选择数据目录
-> 选择导出文件位置
-> 后台线程启动任务
-> 扫描支持文件
-> 自动跳过非账单模板文件
-> 按文件名识别平台和店铺
-> 识别来源模板
-> 调用对应解析器
-> 标准化时间、金额、备注
-> 分类映射
-> 跳过忽略记录
-> 生成分类汇总表
-> 导出 Excel
-> 如存在未映射项则弹窗提醒
-> 返回运行结果
```

## 5. 解析设计

### 5.1 文件名解析
从文件名提取：

- `platform`
- `store_name`

当前支持平台关键词：

- `拼多多`
- `淘宝`
- `天猫`
- `淘工厂`
- `淘农场`
- `京东`

说明：
- 当前 `京东` 仅完成基础文件名识别，尚无完整模板规则
- `天猫 / 淘工厂 / 淘农场` 共用淘宝系解析与分类

### 5.2 来源识别

当前按“模板类型”识别，不再将平台与扩展名强绑定：

- `pdd_table`
- `tb_table`

识别依据：

- 文件扩展名
- 表头特征
- sheet 优先级

### 5.3 解析器

#### 拼多多模板
- 支持 `CSV/XLS/XLSX`
- 自动定位表头
- 读取字段：
  - `商户订单号`
  - `发生时间`
  - `收入金额（+元）`
  - `支出金额（-元）`
  - `账务类型`
  - `备注`
  - `业务描述`

#### 淘宝系模板
- 支持 `CSV/XLS/XLSX`
- 优先读取 `原始数据` sheet
- 自动定位表头
- 读取字段：
  - `商户订单号`
  - `入账时间`
  - `收入（+元）`
  - `支出（-元）`
  - `账务类型`
  - `备注`
  - `业务描述`

## 6. 标准化设计

### 6.1 月份

- 拼多多：从 `发生时间` 提取 `YYYY-MM`
- 淘宝系：从 `入账时间` 提取 `YYYY-MM`

### 6.2 金额

内部统一为单个 signed amount：

- 收入：正数
- 支出：负数

异常规则：

- 收入和支出同时为空或为 `0`：记异常并跳过
- 收入和支出同时有值：记异常并跳过

### 6.3 淘宝备注归一化

目标：
- 去掉动态编号、订单号、邮箱等噪音
- 保留可用于分类的稳定文本

该规则同样适用于：
- `天猫`
- `淘工厂`
- `淘农场`

## 7. 分类设计

### 7.1 淘宝系

分类键：

`备注归一化 —— 业务描述`

特殊规则：

- `[空] —— [空]` 记为忽略
- 不进入汇总
- 未命中映射时自动归类为：
  - `明细分类 = 暂未分类`
  - `大类 = 暂未分类`

### 7.2 拼多多

分类键优先使用：

- `业务描述`

说明：
- `虚假发货`、`运费补偿` 已归入 `售后费用`
- 未命中映射时同样归类为 `暂未分类 / 暂未分类`

### 7.3 未映射提醒

当记录未命中分类规则时：

- 仍参与汇总
- 在导出结果中显示 `暂未分类`
- 生成提醒文案并在导出完成后弹窗

提醒文案格式：

```text
店铺XXX账单表格中的业务描述项：xxx 未做分类映射，请及时联系软件开发方维护更新，以便于后续的统计工作。
```

## 8. 汇总设计

当前导出采用分类汇总长表，每行代表一个分类维度组合：

- `月份`
- `店铺名`
- `平台`
- `大类`
- `明细分类`
- `收入金额`
- `支出金额`
- `记录数`

### 8.1 贷借法输出规则

内部仍使用单个 signed amount 计算，但导出时拆分为两列：

- 如果金额大于 0，则：
  - `收入金额 = amount`
  - `支出金额 = 0`
- 如果金额小于 0，则：
  - `收入金额 = 0`
  - `支出金额 = abs(amount)`

### 8.2 记录数定义

`记录数` 指参与当前汇总行统计的原始明细条数：

- 成功解析且成功分类的记录计入
- 被忽略的 `[空] —— [空]` 不计入
- 金额异常记录不计入
- `暂未分类` 记录计入

## 9. UI 设计

### 9.1 主界面

包含：

- 数据目录输入框
- 浏览按钮
- 一键生成报表按钮
- 状态文本
- 进度文本
- 进度条
- 处理结果文本
- 导出路径文本
- 异常信息列表
- `复制选中`
- `复制全部`

### 9.2 长任务处理

- 报表生成在后台线程执行
- 主线程只负责 UI 刷新
- 处理期间禁用目录选择和启动按钮
- 界面保持响应，不再出现明显“未响应”

## 10. 导出设计

### 10.1 导出方式

- 每次运行前弹出保存对话框
- 用户自行选择导出位置和文件名

### 10.2 导出文件

- 单个 Excel 文件
- 单个 sheet：`总汇总表`

### 10.3 导出后的提醒

- 若存在未映射项：弹出警告窗口
- 若无未映射项：弹出普通完成提示

## 11. 数据存储

SQLite 当前保存：

- `app_config`
- `run_logs`

主要用途：

- 默认输入目录
- 最近一次导出目录
- 运行日志

## 12. 打包与发布

### 12.1 桌面程序

- 使用 `main.pyw`
- 通过 `pythonw` / `PyInstaller --windowed` 启动
- 默认不显示黑色命令行窗口

### 12.2 安装包

- 使用 `PyInstaller` 打出程序目录
- 使用 `Inno Setup` 打成单文件安装包
- 当前版本号：`V1.2 / 1.2.0`

### 12.3 升级安装

- 安装器沿用同一个 `AppId`
- 新版本安装时会识别旧版本
- 用户安装 `V1.2` 时会覆盖升级旧版本

## 13. 当前验证结论

当前版本已经完成并验证：

- PySide6 界面可正常启动
- 可读取淘宝系和拼多多真实样本
- 同平台支持 `CSV/XLS/XLSX`
- 可导出包含 `大类` 和 `明细分类` 的长表
- 可按贷借法输出 `收入金额 / 支出金额`
- 未匹配映射会进入 `暂未分类`
- 导出后会弹出未映射提醒
- 异常信息支持复制
- 大批量数据处理时界面保持可响应并显示进度
- 已生成 `V1.2` 安装包并支持升级安装

## 14. 后续建议

建议后续继续推进：

1. 把分类规则从代码迁到 Excel 或数据库配置
2. 将金额为 `0` 的空记录自动过滤，减少异常提示
3. 增加导出后自动打开目录
4. 增加规则管理界面
